docker run ubuntu:latest

docker run (docker create  + docker start )

-it (Interactive session)

docker start sweet_banzai (позволяет запустить существующий контейнер) можно брать или имя контейнера, или ID контейнера

docker rm -f tender_brattain (docker remove force)

docker cp